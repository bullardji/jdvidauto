## Cursor Cloud specific instructions

This repository is currently a placeholder with no application code, dependencies, or build system.

- **Contents**: A single file `newdoc` containing the text "test".
- **No services to run**: There are no applications, servers, or background services.
- **No dependencies**: No `package.json`, `requirements.txt`, `go.mod`, or any other dependency manifest exists.
- **No lint/test/build**: No linter, test runner, or build tool is configured.

When application code is added, update this section with:
- How to install dependencies
- How to run lint, tests, and the dev server
- Any non-obvious startup caveats
